package Interfaces;

public interface CountableI extends ItemI {
	
	// implement from ItemI
	
	// getCost()
	// formula: 
	// volume (lt) * cost(depends which item it is)
	
	// getPrice()
	// formula:
	// volume (lt) * price(depends which item it is)
	
}
