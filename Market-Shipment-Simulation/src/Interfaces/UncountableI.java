package Interfaces;

public interface UncountableI extends ItemI {

	// implement from ItemI
	
	// getCost()
	// calculate the cost of the item by using the formula: 
	// cost(depends which item it is) * mass (kg)
	
	// getPrice()
	// calculate the price of the item by using the formula:
	// volume (lt) * price (depends which item it is)
}
